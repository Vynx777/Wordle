<Window x:Class="Wordle.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wordle"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="600">
    <Window.Resources>
        <ControlTemplate x:Key="CharBlockTemplate" TargetType="{x:Type ContentControl}">
            <Grid>
                <Rectangle HorizontalAlignment="Stretch" Stroke="Black" VerticalAlignment="Stretch"/>
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                  Content="{TemplateBinding Content}">
                    <ContentPresenter.ContentTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="42" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </ContentPresenter.ContentTemplate>
                </ContentPresenter>
            </Grid>
        </ControlTemplate>
    </Window.Resources>
    <Grid x:Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ContentControl Template="{DynamicResource CharBlockTemplate}" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="15,5"/>
        <TextBox Grid.Column="0" Grid.ColumnSpan="5" HorizontalAlignment="Center" Height="36" Grid.Row="6" TextWrapping="Wrap" TextAlignment="Center" VerticalContentAlignment="Center" Text="TextBox" VerticalAlignment="Center" Width="204"/>
        <Button Grid.Column="0" Grid.ColumnSpan="5" Content="Submit" HorizontalAlignment="Center" Height="36" Grid.Row="7" VerticalAlignment="Center" Width="204" Click="Button_Click"/>

    </Grid>
</Window>
